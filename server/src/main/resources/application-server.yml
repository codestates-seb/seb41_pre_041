spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create-drop
jwt:
  key: ${JWT_KEY}
  ATExpiration: ${JWT_AT_EXPIRATION}
  RTExpiration: ${JWT_RT_EXPIRATION}